import styled from "styled-components";

const Header = styled.h1``;

const Title = styled(Header)``;
const Subtitle = styled(Header)``;

const Button = styled.button``;

export { Button, Header, Title, Subtitle };